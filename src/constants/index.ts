export const PATHS = {
  SEARCH_PAGE: '/',
  FAVORITES_PAGE: 'favorites',
  VACANCY_PAGE: 'vacancy',
  EMPTY_PAGE: 'empty',
} as const;

export const FONTS = {
  MAIN: 'Inter',
  PRIMARY: 'Poppins',
} as const;

export const COLORS = {
  GREY_SCALE: 'grey-scale',
  BLUE_SCALE: 'blue-scale',
} as const;

export const SIZES = {
  HEADER_HEIGHT: 84,
} as const;
